services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: node-sequelize-app
    ports:
      - "5000:5000"
    env_file:
      - .env.development  # Automatically extracts all variables inside .env.development
    restart: unless-stopped